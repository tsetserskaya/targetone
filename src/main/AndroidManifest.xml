<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.targetone">

    <!-- Разрешаем приложению работать с Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity
            android:name=".RangeCategoryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AboutActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".FeedbackActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".BuglerActivity" />
        <activity android:name=".ObjectCategoryActivity" />
        <activity android:name=".LightActivity" />
        <activity android:name=".PointActivity" />
        <activity android:name=".BlindageActivity" />
        <activity android:name=".WellActivity" />
        <activity android:name=".RainbowActivity" />
        <activity android:name=".PagerActivity" />
        <activity android:name=".MoonwalkerActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecyclerViewActivity">
        </activity>
    </application>

</manifest>